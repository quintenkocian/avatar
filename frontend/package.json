{
  "name": "avatar-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Avatar — visitor chat + admin dashboard for a digital-twin web app",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
